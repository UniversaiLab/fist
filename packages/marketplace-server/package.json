{
  "name": "@fist/marketplace-server",
  "version": "0.2.0",
  "private": true,
  "description": "Config marketplace backend (creator accounts, listings, purchases) on Bun + Hono + Redis, with mocked payment processing -- no real money moves through this service.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "bun run src/server.js",
    "dev": "bun --watch src/server.js"
  },
  "dependencies": {
    "hono": "^4.6.14"
  }
}
